	类中域为基本类型时能够自动初始化为0, 对象引用会被初始化为null, 而且你试图为他们调用任何任何方法都会得到一个异常--运行时错误, 但在不抛出异常的情况下依旧可以打印一个null引用
	编译器并不是简单的为每一个引用都创建默认对象, 想初始化这些引用, 可以在以下位置进行
	1.在定义对象的地方, 这意味着它们总是能够在构造函数调用之前    被初始化
	2.在类的构造函数里面
	3.在正要使用这些引用之前, 这种方法属于惰性初始化在生成对象不值得或不必每次都生成对象的情况下, 这种方式可以减少额外的负担